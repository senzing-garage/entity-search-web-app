{
  "admin": {
    "mode": "${d.adminAuthMode}",
    "checkUrl": "${d.adminAuthStatusUrl}",
    "redirectOnFailure": false,
    "loginUrl": "${d.adminAuthRedirectUrl}"
  },
  "operator": {
    "mode": "${d.operatorAuthMode}",
    "checkUrl": "${d.operatorAuthStatusUrl}",
    "redirectOnFailure": false,
    "loginUrl": "${d.operatorAuthRedirectUrl}"
  }
}
